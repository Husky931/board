    # print(len(resultsTrimmed), type(resultsTrimmed))
  # def parse_details(self, response):
  #   results = json.loads(response.text)
  #   print(results)
  #   details = {}
    # details["title"] = results["jobSearchResponse"]["data"][0]["title"]
    # details["redirectUrl"] = results["redirectUrl"]["data"][0]["redirectUrl"]
    # details["locations"] = results["jobSearchResponse"]["data"][0]["locations"]
    # print(results["jobSearchResponse"]["data"][0]["title"])

    # print(results["jobSearchResponse"])
    # filename = f'jobs17.html'
    # with open(filename, 'wb') as f:
    #   f.write(bytes(str(results["jobSearchResponse"]["data"]),'utf8'))
    # self.log(f'Saved file {filename}')